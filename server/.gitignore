node_modules/
dist/
dev.db
dev.db-journal
test.db
test.db-journal
.env
# 驱动与压测运行时：随项目存在但不提交。
# 必须用 / 前缀锚定到 server/ 根，否则会连带忽略 src/engine/jmeter/ 等源码目录
/bin/
# 内置 JMeter 压测运行时（与 bin/chromedriver 同策略）
/jmeter/
# 压测工作目录：原始 JTL + JMeter 官方 HTML 报告（按报告 id 分目录，删除报告时清理）
/.perf-runs/
