开发文档：


## 第一部分：技术栈与架构约定

### 1.1 技术栈

前端：

- 框架：Vue 3 + TypeScript
- 组件库：Ant Design Vue 4.x
- 状态管理：Pinia
- 构建工具：Vite
- 拖拽库：vuedraggable（基于 SortableJS）
- 代码编辑器：Monaco Editor
- 树组件：基于 Ant Design Vue Tree 二次封装，支持虚拟滚动

后端：

- 语言：Java 17 / Spring Boot 3.x
- 数据库：MySQL 8.0
- ORM：MyBatis-Plus
- 缓存：Redis 7.x
- 消息队列：RabbitMQ（用于任务异步执行）
- 定时任务：Quartz Scheduler（集群模式）
- API 风格：RESTful

### 1.2 架构约定

- 前后端分离，前端通过 RESTful API 与后端交互
- 执行引擎为独立模块，任务通过 RabbitMQ 异步投递，执行机通过心跳注册到资源池
- 所有调试记录、执行报告的请求体/响应体，超过 1MB 时存储到 OSS/MinIO，数据库只存引用路径
- 所有时间字段统一使用 UTC 存储，前端展示时转换为本地时区


## 第二部分：数据模型定义

### 2.1 核心实体及字段

**Project（项目）**

- id: Long 主键
- name: String 项目名称，必填，唯一
- description: String 项目描述
- createdAt: DateTime 创建时间
- updatedAt: DateTime 更新时间

**Module（目录/模块）**

- id: Long 主键
- projectId: Long 所属项目 ID
- parentId: Long 父目录 ID，null 表示根目录
- name: String 目录名称，必填
- type: Enum 目录类型：CASE（用例目录）/ API（接口目录）
- sortOrder: Int 排序号
- createdAt / updatedAt: DateTime

**CaseInfo（用例）**

- id: Long 主键
- moduleId: Long 所属目录 ID
- name: String 用例名称，必填
- status: Enum 用例状态：DRAFT（草稿）/ COMPLETED（已完成）/ DEPRECATED（已废弃）
- priority: Enum 优先级：P0 / P1 / P2 / P3
- tags: String 标签，逗号分隔存储
- isPublic: Boolean 是否公共用例
- version: Int 当前版本号
- createdAt / updatedAt: DateTime
- deletedAt: DateTime 软删除时间，null 表示未删除

**CaseStep（用例步骤）**

- id: Long 主键
- caseId: Long 所属用例 ID
- parentId: Long 父步骤 ID，null 表示顶层步骤
- stepType: Enum 步骤类型：HTTP_REQUEST / SCRIPT / WAIT / VARIABLE_ASSIGN / IF_CONTROLLER / FOR_CONTROLLER / WHILE_CONTROLLER / TRANSACTION_CONTROLLER / ONCE_CONTROLLER / REF_PUBLIC_CASE
- name: String 步骤名称
- position: Enum 位置：PRE（前置）/ TEST（测试）/ POST（后置）
- sortOrder: Int 排序号
- enabled: Boolean 是否启用，默认 true
- config: JSON 步骤配置，JSON 结构根据 stepType 不同而不同
- createdAt / updatedAt: DateTime

**CaseVersion（用例版本快照）**

- id: Long 主键
- caseId: Long 所属用例 ID
- version: Int 版本号
- snapshot: JSON 完整用例快照（含所有步骤）
- changeSummary: String 变更摘要
- createdBy: Long 保存人 ID
- createdAt: DateTime

**ApiDefinition（接口定义）**

- id: Long 主键
- projectId: Long 所属项目 ID
- moduleId: Long 所属模块 ID
- name: String 接口名称
- method: Enum 请求方法：GET / POST / PUT / DELETE / PATCH / HEAD / OPTIONS
- url: String 请求路径
- pathParams: JSON 路径参数定义
- queryParams: JSON Query 参数定义
- headers: JSON 请求头定义
- bodyType: Enum 请求体类型：NONE / FORM_DATA / X_WWW_FORM_URLENCODED / JSON / XML / BINARY
- bodySchema: JSON 请求体 Schema
- responseExamples: JSON 响应示例，按状态码存储
- description: String 接口描述
- mockEnabled: Boolean 是否启用 Mock
- createdAt / updatedAt: DateTime
- deletedAt: DateTime

**Environment（环境）**

- id: Long 主键
- projectId: Long 所属项目 ID
- name: String 环境名称，必填
- variables: JSON 环境变量，键值对存储
- createdAt / updatedAt: DateTime

**GlobalVariable（全局变量）**

- id: Long 主键
- projectId: Long 所属项目 ID
- name: String 变量名，必填，同项目内唯一
- type: Enum 变量类型：STRING / NUMBER / BOOLEAN / TIMESTAMP / RANDOM / SECRET
- value: String 变量值，SECRET 类型加密存储
- description: String 变量描述
- createdAt / updatedAt: DateTime

**DebugRecord（调试记录）**

- id: Long 主键
- caseId: Long 用例 ID
- caseNameSnapshot: String 用例名称快照
- environmentId: Long 执行环境 ID
- executeMode: Enum 执行方式：LOCAL（本地）/ SERVER（服务端）/ SCHEDULED（定时）/ CI（CI 触发）
- result: Enum 执行结果：SUCCESS / FAILED / SKIPPED
- totalDuration: Long 总耗时（毫秒）
- requestData: JSON 各步骤请求详情
- responseData: JSON 各步骤响应详情
- assertionResults: JSON 断言结果
- extractedVariables: JSON 提取变量
- errorLog: Text 错误日志
- createdBy: Long 执行人 ID
- createdAt: DateTime

**TestTask（测试任务）**

- id: Long 主键
- projectId: Long 所属项目 ID
- name: String 任务名称，必填
- caseIds: JSON 关联用例 ID 及排序
- environmentId: Long 执行环境 ID
- executeMode: Enum 执行模式：SERIAL（串行）/ PARALLEL（并行）
- parallelCount: Int 并发数（并行模式时有效）
- globalTimeout: Long 全局超时（秒）
- caseTimeout: Long 单用例超时（秒）
- retryCount: Int 重试次数
- retryInterval: Long 重试间隔（秒）
- failStrategy: Enum 失败策略：STOP_ALL（立即停止）/ CONTINUE（继续执行）
- scheduleEnabled: Boolean 是否启用定时
- cronExpression: String Cron 表达式
- createdAt / updatedAt: DateTime

**TestReport（测试报告）**

- id: Long 主键
- taskId: Long 任务 ID
- taskNameSnapshot: String 任务名称快照
- totalCount: Int 总用例数
- successCount: Int 成功数
- failedCount: Int 失败数
- skippedCount: Int 跳过数
- passRate: Decimal 通过率
- startTime: DateTime 开始时间
- endTime: DateTime 结束时间
- duration: Long 总耗时
- details: JSON 各用例执行详情
- createdAt: DateTime


## 第三部分：API 接口规范

### 3.1 通用约定

- 基础路径：/api/v1
- 请求头：Content-Type: application/json；Authorization: Bearer {token}
- 响应格式统一为：
  - 成功：{ "code": 0, "message": "success", "data": { } }
  - 失败：{ "code": 错误码, "message": "错误描述", "data": null }
- 分页请求参数：pageNum（从 1 开始）、pageSize（默认 20，最大 200）
- 分页响应格式：{ "code": 0, "message": "success", "data": { "list": [], "total": 100, "pageNum": 1, "pageSize": 20 } }

### 3.2 核心 API 列表

**全局变量管理**

- POST /api/v1/variables —— 新增变量
- PUT /api/v1/variables/{id} —— 编辑变量
- DELETE /api/v1/variables/{id} —— 删除变量
- POST /api/v1/variables/batch-delete —— 批量删除，请求体 { ids: [1,2,3] }
- GET /api/v1/variables?pageNum=1&pageSize=20&keyword=xxx&type=STRING —— 分页查询
- POST /api/v1/variables/import —— 批量导入，multipart/form-data 上传文件
- GET /api/v1/variables/export —— 批量导出，返回文件流
- POST /api/v1/variables/batch-replace —— 批量替换，请求体 { findText: "xxx", replaceText: "yyy", scope: "ALL" }
- GET /api/v1/variables/{id}/references —— 查看变量引用位置

**用例管理**

- POST /api/v1/cases —— 新建用例
- PUT /api/v1/cases/{id} —— 编辑用例（自动生成版本快照）
- DELETE /api/v1/cases/{id} —— 删除用例（软删除到回收站）
- GET /api/v1/cases/{id} —— 查询用例详情
- GET /api/v1/cases/tree?projectId=1 —— 获取用例目录树
- POST /api/v1/cases/batch-move —— 批量移动，请求体 { caseIds: [], targetModuleId: 1 }
- POST /api/v1/cases/batch-copy —— 批量复制，请求体 { caseIds: [], targetModuleId: 1, namePrefix: "", nameSuffix: "" }
- POST /api/v1/cases/batch-delete —— 批量删除
- POST /api/v1/cases/batch-status —— 批量修改状态，请求体 { caseIds: [], status: "DEPRECATED" }
- POST /api/v1/cases/batch-tag —— 批量打标签，请求体 { caseIds: [], tags: ["回归"] }
- GET /api/v1/cases/{id}/versions —— 获取版本列表
- POST /api/v1/cases/{id}/versions/{versionId}/rollback —— 版本回退
- GET /api/v1/cases/{id}/versions/compare?versionA=1&versionB=2 —— 版本对比

**用例步骤管理**

- POST /api/v1/cases/{caseId}/steps —— 新增步骤
- PUT /api/v1/cases/{caseId}/steps/{stepId} —— 编辑步骤
- DELETE /api/v1/cases/{caseId}/steps/{stepId} —— 删除步骤
- PUT /api/v1/cases/{caseId}/steps/reorder —— 调整步骤顺序，请求体 { stepIds: [3,1,2] }
- POST /api/v1/cases/steps/copy —— 复制步骤（支持跨用例），请求体 { sourceStepId: 1, targetCaseId: 2, targetPosition: "TEST" }

**调试**

- POST /api/v1/debug/execute —— 执行调试，请求体 { caseId: 1, environmentId: 2, tempVariables: {}, mode: "FULL" }
- GET /api/v1/debug/records?caseId=1&result=SUCCESS&pageNum=1&pageSize=20 —— 查询调试记录
- GET /api/v1/debug/records/{id} —— 查询调试记录详情
- POST /api/v1/debug/records/{id}/replay —— 一键重放
- POST /api/v1/debug/records/compare —— 对比，请求体 { recordIdA: 1, recordIdB: 2 }
- POST /api/v1/debug/records/generate-case —— 基于调试记录生成用例，请求体 { recordId: 1, targetModuleId: 2 }
- DELETE /api/v1/debug/records/batch —— 批量删除调试记录

**回收站**

- GET /api/v1/recycle-bin?type=CASE&pageNum=1&pageSize=20 —— 查询回收站
- POST /api/v1/recycle-bin/restore —— 还原，请求体 { ids: [1,2], type: "CASE" }
- DELETE /api/v1/recycle-bin/permanent —— 永久删除，请求体 { ids: [1,2], type: "CASE" }
- PUT /api/v1/recycle-bin/config —— 配置自动清理周期，请求体 { days: 30 }

**接口管理**

- POST /api/v1/apis —— 新建接口定义
- PUT /api/v1/apis/{id} —— 编辑接口定义
- DELETE /api/v1/apis/{id} —— 删除接口定义
- GET /api/v1/apis?moduleId=1&keyword=xxx&pageNum=1&pageSize=20 —— 分页查询
- POST /api/v1/apis/import/swagger-url —— Swagger URL 导入，请求体 { url: "http://xxx/swagger.json" }
- POST /api/v1/apis/import/swagger-file —— Swagger 文件上传，multipart/form-data
- POST /api/v1/apis/import/excel —— Excel 导入，multipart/form-data
- GET /api/v1/apis/export?format=EXCEL —— 批量导出
- POST /api/v1/apis/sync —— 接口同步，请求体 { sourceId: 1 }
- POST /api/v1/apis/{id}/generate-case —— 一键生成用例，请求体 { targetModuleId: 1 }

**Mock 管理**

- PUT /api/v1/apis/{id}/mock —— 配置 Mock，请求体 { enabled: true, rules: [ { condition: {}, response: {} } ] }
- GET /mock/{apiId}/** —— Mock 请求入口（独立服务）

**测试任务**

- POST /api/v1/tasks —— 创建任务
- PUT /api/v1/tasks/{id} —— 编辑任务
- DELETE /api/v1/tasks/{id} —— 删除任务
- GET /api/v1/tasks/{id} —— 查询任务详情
- POST /api/v1/tasks/{id}/run —— 运行任务
- POST /api/v1/tasks/{id}/cancel —— 取消任务
- POST /api/v1/tasks/{id}/case-sort —— 用例排序，请求体 { caseIds: [3,1,2] }
- GET /api/v1/tasks/{id}/history —— 查询执行历史

**测试报告**

- GET /api/v1/reports/{id} —— 查询报告详情
- GET /api/v1/reports?taskId=1&pageNum=1&pageSize=20 —— 分页查询报告
- GET /api/v1/reports/{id}/export?format=PDF —— 导出报告
- POST /api/v1/reports/{id}/share —— 生成分享链接，请求体 { expireDays: 7, password: "" }
- GET /api/v1/reports/trend?taskId=1&days=30 —— 查询趋势数据

**权限与审计**

- GET /api/v1/audit-logs?userId=1&actionType=EDIT&pageNum=1&pageSize=20 —— 查询审计日志
- POST /api/v1/audit-logs/export —— 导出审计日志
- GET /api/v1/members?projectId=1 —— 查询项目成员
- POST /api/v1/members —— 添加成员
- PUT /api/v1/members/{id}/role —— 修改成员角色
- DELETE /api/v1/members/{id} —— 移除成员

**用例评审**

- POST /api/v1/reviews —— 提交评审
- GET /api/v1/reviews?status=PENDING&pageNum=1&pageSize=20 —— 查询评审列表
- PUT /api/v1/reviews/{id}/approve —— 通过评审
- PUT /api/v1/reviews/{id}/reject —— 驳回评审
- POST /api/v1/reviews/{id}/comment —— 发表评审意见


## 第四部分：关键逻辑精确定义

### 4.1 变量解析算法

输入：字符串 text、当前执行上下文 context
输出：替换后的字符串

解析规则（伪代码）：

```
function resolveVariables(text, context):
    pattern = /\$\{([a-zA-Z_][a-zA-Z0-9_]*)\}/g
    result = text
    for each match in text.matchAll(pattern):
        varName = match[1]
        value = resolveVariableValue(varName, context)
        if value != null:
            result = result.replace(match[0], value.toString())
        else:
            log.warn("Variable not found: " + varName)
            // 保留原样
    return result

function resolveVariableValue(varName, context):
    // 按优先级查找
    if context.tempVariables.contains(varName):
        return context.tempVariables[varName]
    if context.taskVariables.contains(varName):
        return context.taskVariables[varName]
    if context.environmentVariables.contains(varName):
        return context.environmentVariables[varName]
    if context.globalVariables.contains(varName):
        return context.globalVariables[varName]
    return null
```

嵌套变量解析：先解析内层 ${...}，再解析外层。例如 ${env_${system}}，先解析 ${system} 得到值 test，再解析 ${env_test}。

### 4.2 用例执行流程

```
executeCase(caseId, context):
    // 1. 加载用例及步骤
    case = loadCase(caseId)
    if case.status == DEPRECATED:
        markAsSkipped("用例状态为已废弃")
        return

    // 2. 执行前置步骤
    for step in case.steps.where(position == PRE and enabled):
        result = executeStep(step, context)
        if result.failed and step.failStrategy == STOP:
            executePostSteps(case, context)
            return buildFailedResult(result)

    // 3. 执行测试步骤
    for step in case.steps.where(position == TEST and enabled):
        result = executeStep(step, context)
        if result.failed and step.failStrategy == STOP:
            executePostSteps(case, context)
            return buildFailedResult(result)

    // 4. 执行后置步骤（无论成功失败都执行）
    executePostSteps(case, context)

    // 5. 返回结果
    return buildSuccessResult()
```

### 4.3 断言执行逻辑

```
executeAssertion(assertion, response, context):
    actualValue = extractActualValue(assertion.type, assertion.expression, response)

    switch assertion.type:
        case STATUS_CODE:
            passed = compare(actualValue, assertion.operator, assertion.expectedValue)
        case CONTAINS:
            passed = response.body.contains(assertion.expectedValue)
        case JSONPATH:
            actualValue = jsonPath(response.body, assertion.expression)
            passed = compare(actualValue, assertion.operator, assertion.expectedValue)
        case REGEX:
            pattern = new RegExp(assertion.expression)
            passed = pattern.test(response.body)
        case XPATH:
            actualValue = xpath(response.body, assertion.expression)
            passed = compare(actualValue, assertion.operator, assertion.expectedValue)
        case EMPTY:
            actualValue = jsonPath(response.body, assertion.expression)
            passed = (actualValue == null or actualValue == "")
        case RESPONSE_TIME:
            passed = response.duration <= assertion.expectedValue

    if not passed:
        recordAssertionFailure(assertion.failureMessage, assertion.expectedValue, actualValue)
        if assertion.failStrategy == STOP_CASE:
            throw StopCaseException("断言失败，停止用例执行")
```

### 4.4 流程控制器执行逻辑

IF-ELSE 控制器：

```
executeIfController(controller, context):
    condition = resolveVariables(controller.condition, context)
    if evaluateCondition(condition):
        executeChildSteps(controller.thenSteps, context)
    else if controller.elseSteps != null:
        executeChildSteps(controller.elseSteps, context)
```

FOR 循环控制器：

```
executeForController(controller, context):
    loopCount = controller.loopCount
    loopVariable = controller.loopVariable
    for i in range(loopCount):
        context.setVariable(loopVariable, i)
        executeChildSteps(controller.childSteps, context)
```

WHILE 循环控制器：

```
executeWhileController(controller, context):
    iteration = 0
    maxIterations = controller.maxIterations  // 必填，防止死循环
    while evaluateCondition(controller.condition, context):
        if iteration >= maxIterations:
            log.warn("WHILE 循环达到最大次数，强制退出")
            break
        executeChildSteps(controller.childSteps, context)
        iteration++
```

事务控制器：

```
executeTransactionController(controller, context):
    transactionResult = executeChildSteps(controller.childSteps, context)
    if transactionResult.hasFailure:
        markTransactionAsFailed(controller.name)
    else:
        markTransactionAsSuccess(controller.name)
```

### 4.5 Mock 匹配逻辑

```
handleMockRequest(apiId, request):
    mockConfig = loadMockConfig(apiId)
    if mockConfig == null or not mockConfig.enabled:
        return 404

    // 条件匹配
    for rule in mockConfig.rules:
        if matchCondition(rule.condition, request):
            response = buildMockResponse(rule.response)
            // 变量替换
            response = resolveVariables(response, context)
            return response

    // 默认响应
    if mockConfig.defaultResponse != null:
        return resolveVariables(mockConfig.defaultResponse, context)

    return 404
```

### 4.6 任务执行调度逻辑

```
executeTask(taskId):
    task = loadTask(taskId)
    cases = loadCasesByIds(task.caseIds)

    // 过滤：跳过禁用和废弃用例
    executableCases = cases.filter(c => c.status != DEPRECATED and c.enabled)
    skippedCases = cases.filter(c => c.status == DEPRECATED or not c.enabled)
    logSkippedCases(skippedCases)

    // 执行模式
    if task.executeMode == SERIAL:
        for case in executableCases:
            result = executeCaseWithRetry(case, task)
            if result.failed and task.failStrategy == STOP_ALL:
                break
    else:  // PARALLEL
        threadPool = createThreadPool(task.parallelCount)
        for case in executableCases:
            threadPool.submit(() -> executeCaseWithRetry(case, task))
        threadPool.awaitTermination(task.globalTimeout)

    // 生成报告
    report = generateReport(task, results)
    saveReport(report)
    sendNotification(report)
```

### 4.7 调试记录差异对比算法

```
compareDebugRecords(recordA, recordB):
    differences = []

    // 对比请求差异
    for step in recordA.steps:
        stepB = recordB.steps.find(s => s.id == step.id)
        if stepB == null:
            differences.add({ type: "STEP_REMOVED", stepName: step.name })
        else:
            if step.request != stepB.request:
                differences.add({ type: "REQUEST_CHANGED", stepName: step.name, before: step.request, after: stepB.request })
            if step.response != stepB.response:
                differences.add({ type: "RESPONSE_CHANGED", stepName: step.name, before: step.response, after: stepB.response })
            if step.assertionResults != stepB.assertionResults:
                differences.add({ type: "ASSERTION_CHANGED", stepName: step.name, before: step.assertionResults, after: stepB.assertionResults })
            if step.extractedVariables != stepB.extractedVariables:
                differences.add({ type: "VARIABLE_CHANGED", stepName: step.name, before: step.extractedVariables, after: stepB.extractedVariables })

    return differences
```

### 4.8 回收站自动清理逻辑

```
autoCleanRecycleBin():
    config = loadCleanupConfig()
    if config.days == null:
        return  // 不自动清理

    threshold = now() - config.days * 24 * 60 * 60 * 1000
    items = queryRecycleBin(deletedAt < threshold)
    for item in items:
        permanentlyDelete(item)
```

### 4.9 操作审计日志记录逻辑

```
recordAuditLog(action, userId, objectType, objectId, beforeData, afterData):
    log = {
        userId: userId,
        action: action,  // CREATE / UPDATE / DELETE / DEBUG / EXECUTE / REVIEW
        objectType: objectType,  // CASE / MODULE / API / VARIABLE / TASK / ENVIRONMENT
        objectId: objectId,
        beforeData: beforeData,  // 变更前数据（JSON）
        afterData: afterData,    // 变更后数据（JSON）
        ipAddress: getRequestIP(),
        createdAt: now()
    }
    saveAuditLog(log)
```


## 第五部分：前端页面与交互精确描述

### 5.1 整体布局结构

页面采用 CSS Flexbox 布局：

- 最外层容器：display: flex; flex-direction: column; height: 100vh;
- 顶部栏：固定高度 48px，flex-shrink: 0
- 分割线：height: 1px; background: #e8e8e8;
- 下方三栏容器：display: flex; flex: 1; overflow: hidden;
- 左侧面板：默认宽度 300px，最小 220px，flex-shrink: 0
- 中间面板：flex: 1，最小宽度 400px
- 右侧面板：默认宽度 350px，最小 280px，flex-shrink: 0

拖拽调整宽度：使用 vuedraggable 或自定义拖拽手柄，手柄宽度 4px，悬停高亮。

折叠/展开：每个面板头部有折叠按钮，折叠后面板宽度变为 32px，只展示图标。

### 5.2 目录树组件

使用 Ant Design Vue 的 Tree 组件，配置：

- showLine: true
- draggable: true
- 右键菜单：使用 contextmenu 事件，自定义菜单组件
- 虚拟滚动：当节点数超过 500 时启用 virtual 模式

树节点数据结构：

```
{
    key: "module-1",           // 唯一标识
    title: "用户模块",          // 显示名称
    type: "module",            // module / case
    isLeaf: false,
    children: [
        {
            key: "case-1",
            title: "用户登录用例",
            type: "case",
            status: "COMPLETED",
            isLeaf: true,
            children: []
        }
    ]
}
```

### 5.3 用例编辑区

布局：

- 顶部：用例名称输入框（Ant Design Input，带保存按钮和调试按钮）
- Tab 栏：脚本 / 调试记录
- 脚本 Tab 内部三个区域：
  - 前置步骤区：标题"前置步骤"+ 添加按钮，虚线边框容器
  - 测试步骤区：标题"测试步骤"+ 添加按钮，虚线边框容器，占最大空间
  - 后置步骤区：标题"后置步骤"+ 添加按钮，虚线边框容器

步骤卡片：

- 使用 Ant Design Card 组件
- 头部：步骤类型图标 + 步骤名称 + 启用/禁用开关 + 折叠箭头 + 右键菜单按钮
- 体部：根据步骤类型渲染不同配置表单
- 拖拽：使用 vuedraggable 包裹步骤列表

### 5.4 调试弹窗

使用 Ant Design Modal 组件，宽度 900px：

- 顶部：环境选择下拉框 + 临时变量编辑按钮 + 执行方式切换（本地/服务端）
- 中部：变量预览区，展示所有变量及最终值
- 底部：执行模式按钮组（全速运行/单步调试/断点调试）+ 开始按钮
- 执行中：中部变为实时日志区，展示每一步的执行结果

### 5.5 步骤配置表单（按类型）

HTTP Request 步骤：

- 请求方法下拉框（GET/POST/PUT/DELETE/PATCH/HEAD/OPTIONS）
- URL 输入框（支持 ${变量名}）
- Tab 切换：请求参数 / 请求头 / 请求体 / 断言 / 提取
- 请求参数 Tab：Key-Value 列表（支持批量添加、删除行）
- 请求头 Tab：Key-Value 列表 + "获取请求头"按钮
- 请求体 Tab：根据 bodyType 切换编辑器（JSON 用 Monaco，form-data 用 Key-Value 列表）
- 断言 Tab：断言列表，每条断言含类型下拉框、表达式输入框、比较运算符、预期值、失败提示、失败策略
- 提取 Tab：提取列表，每条提取含提取方式下拉框、表达式、变量名、默认值

Script 步骤：

- 语言选择（JavaScript / Python）
- Monaco Editor 代码编辑器
- 脚本模板自动填入，包含 context 对象访问方法

Wait 步骤：

- 等待方式选择（固定等待 / 条件等待）
- 固定等待：时间输入框（毫秒）
- 条件等待：条件表达式输入框 + 最大等待时间 + 轮询间隔

Variable Assign 步骤：

- 变量名输入框
- 变量值输入框（支持 ${变量名}）
- 赋值方式（直接赋值 / 表达式计算）

Controller 步骤（IF/FOR/WHILE/事务/仅一次）：

- IF：条件表达式输入框 + 子步骤容器（THEN 区域和 ELSE 区域）
- FOR：循环变量名 + 循环次数 + 子步骤容器
- WHILE：条件表达式 + 最大循环次数（必填）+ 子步骤容器
- 事务：事务名称 + 子步骤容器
- 仅一次：子步骤容器


## 第六部分：错误码定义

错误码 0：HTTP 200，成功

错误码 1001：HTTP 400，参数校验失败
错误码 1002：HTTP 401，未登录或 Token 过期
错误码 1003：HTTP 403，无权限操作
错误码 1004：HTTP 404，资源不存在
错误码 1005：HTTP 409，资源名称冲突（同名已存在）

错误码 2001：HTTP 400，变量名不合法
错误码 2002：HTTP 400，变量被引用，无法删除
错误码 2003：HTTP 400，用例名称为空
错误码 2004：HTTP 400，任务名称为空
错误码 2005：HTTP 400，任务至少需要 1 条用例才能运行
错误码 2006：HTTP 400，循环控制器未配置最大循环次数

错误码 3001：HTTP 400，Swagger 文档解析失败
错误码 3002：HTTP 400，Excel 导入失败，存在非法行
错误码 3003：HTTP 400，请求方式不合法
错误码 3004：HTTP 400，URL 格式不合法

错误码 4001：HTTP 500，调试执行异常
错误码 4002：HTTP 500，任务执行异常
错误码 4003：HTTP 500，Mock 服务异常
错误码 4004：HTTP 500，报告生成失败


## 第七部分：边界条件与异常处理清单

AI 实现时需要覆盖以下边界情况：

变量相关：

- 变量不存在时：日志警告 + 保留原始 ${变量名}（默认行为）
- 嵌套变量超过 5 层：停止解析，日志警告
- 密钥类型变量导出：值显示为 ENCRYPTED
- 删除被引用的变量：返回错误码 2002，列出引用位置

用例相关：

- 删除有子目录的目录：弹窗确认，选择"仅删除目录"或"同步删除子内容"
- 删除有子用例的目录：同上
- 用例名为空：不允许保存，前端提示
- 用例无任何步骤：允许保存，但调试时提示"用例无步骤"
- 复制用例到相同目录：自动在名称后添加"（副本）"
- 版本回退后再次编辑：生成新版本，不回退中丢失

调试相关：

- 调试时变量解析失败：记录警告但继续执行
- 断言失败策略为"停止用例"：停止后续步骤，但后置步骤仍执行
- 后置步骤失败：默认忽略，记录日志
- 提取失败：使用默认值或标记为 null
- 调试记录超过 1MB：截断存储，显示"内容过大已截断"

任务相关：

- 任务包含已废弃用例：跳过并记录
- 任务包含已删除用例：跳过并记录
- 并行执行时共享变量冲突：加锁保护
- 全局超时触发：停止所有执行中的用例，标记为超时失败
- 重试时变量状态：重置该用例的临时变量

Mock 相关：

- Mock 条件匹配不到任何规则：返回默认响应，若无默认响应返回 404
- Mock 响应变量引用不存在：保留原始字符串

回收站相关：

- 还原时原目录已不存在：还原到根目录
- 还原时同名冲突：提示重命名后还原
- 自动清理周期为"不自动清理"：跳过清理任务


## 第八部分：AI 实现任务拆解建议

按以下顺序逐个模块实现，每个模块完成后进行验证再进入下一个：

模块 1：项目基础框架

- 项目初始化（前端 Vue3 + 后端 Spring Boot）
- 数据库表结构创建（基于数据模型定义）
- 用户认证与登录
- 基础布局（顶部栏 + 三栏结构 + 拖拽调整宽度 + 折叠）

模块 2：全局变量管理

- 变量 CRUD API
- 变量管理页面
- 变量引用解析器
- 变量导入导出
- 批量替换

模块 3：用例与目录管理

- 目录 CRUD API
- 目录树组件（含右键菜单）
- 用例 CRUD API
- 用例版本管理
- 批量操作
- 标签体系
- 公共用例引用

模块 4：用例步骤编辑

- 步骤 CRUD API
- 步骤卡片组件
- 各类型步骤配置表单
- 步骤拖拽排序
- 步骤复制粘贴（含跨用例）

模块 5：断言与提取引擎

- 断言类型实现（状态码/包含/JSONPath/正则/XPath/空值/响应时间）
- 提取方式实现（Header/Cookie/JSONPath/正则/XPath）
- 断言和提取的配置 UI

模块 6：流程控制器

- IF-ELSE 控制器
- FOR 循环控制器
- WHILE 循环控制器（含最大次数保护）
- 事务控制器
- 仅一次控制器
- 控制器嵌套执行

模块 7：调试能力

- 调试执行 API
- 调试弹窗
- 单步/断点调试
- 调试记录存储与查询
- 调试记录详情展示
- 调试记录对比
- 一键重放
- 基于调试记录生成用例

模块 8：脚本执行

- JS 脚本执行引擎
- Python 脚本执行引擎
- 脚本编辑器（Monaco）
- 公共脚本库

模块 9：接口管理

- 接口定义 CRUD API
- 接口管理页面
- Swagger 导入（URL + 文件）
- Excel 导入导出
- 接口同步
- Mock 服务
- 一键生成用例

模块 10：测试任务执行

- 任务 CRUD API
- 任务管理页面
- 用例挑选与排序
- 串行/并行执行引擎
- 重试/超时/失败策略
- 定时任务（Quartz）

模块 11：测试报告

- 报告生成
- 报告详情页
- 导出 PDF/HTML
- 分享链接
- 趋势统计

模块 12：企业协作

- 操作审计日志
- 成员权限管理
- 用例评审流程
- 回收站完整功能
- 自动清理定时任务

模块 13：CI/CD 集成

- 任务执行 REST API
- API Token 管理
- Jenkins 集成文档
- 执行回调


## 第九部分：验收标准

每个模块完成时，需通过以下验收：

1. API 接口返回格式符合第三部分规范
2. 边界条件全部通过测试（见第七部分清单）
3. 前端交互符合第五部分描述
4. 错误场景返回正确的错误码和提示信息
5. 所有列表页面支持分页、搜索、排序
6. 所有删除操作有二次确认
7. 所有操作记录审计日志（模块 12 完成后启用）


本文档为 AI 可执行版 PRD V2.0，核心改进在于：

- 补充了技术栈约定，消除 AI 选型的不确定性
- 定义了完整的数据模型，AI 可直接生成表结构
- 规范了 API 接口路径和格式，AI 可直接生成 Controller
- 用伪代码精确定义了关键算法，AI 可直接翻译为实现
- 描述了前端组件选型和布局参数，AI 可直接生成页面
- 列出了边界条件和错误码，AI 可实现完整的异常处理
- 给出了模块化实施顺序，便于迭代开发和验收